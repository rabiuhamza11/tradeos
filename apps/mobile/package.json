{
  "name": "@tradeos/mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "react-native start",
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "build": "echo \"Mobile build handled by EAS\" && exit 0",
    "test": "echo \"No tests for mobile app\" && exit 0",
    "lint": "echo \"No linter for mobile app\" && exit 0"
  }
}